// routes/aiVoiceVideoSupportRoutes.js
const express = require("express");
const router = express.Router();

// Placeholder route
router.get("/", (req, res) => {
  res.send("AI Voice & Video Support API Running...");
});

module.exports = router;
